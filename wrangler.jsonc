// Cloudflare Workers deployment config.
//
// Cloudflare now steers new accounts to Workers rather than Pages, and the
// "import a repository" flow runs `wrangler deploy`. Without this file that
// command has nothing to deploy and the build fails with "error occurred
// while running deploy command".
//
// There is no server-side code here. This is a static site, so there is no
// "main" field: an assets-only Worker is a supported configuration and simply
// serves the directory. _headers and _redirects in the repository root are
// read natively by Workers static assets, exactly as they would be by Pages,
// so the caching, security headers and the www redirect all still apply.
{
  "name": "khaana-website",
  "compatibility_date": "2026-08-01",
  "assets": {
    "directory": "./"
  }
}
